# Validation

Verified 2026-09-22: **32 tests passed on macOS; 32 passed on Linux**.
No tests were skipped; the suite also passed under actual system Python3.9 on macOS.

- macOS: Apple Silicon, Python3.14.6; synthetic fixtures and independent installation.
- Linux: unprivileged Alpine container, Python3.14.7, network disabled during tests.
- Command: `make test`; the documented synthetic demo also passes.
- No agent configuration, original operational state, accounts, or model calls.

Runtime/test snapshot SHA-256: `d1cf8b8206e1e195daef658a7edafbb3913dcd879602cefca6b0c0e5c9aed477`.
The digest covers sorted relative paths, content SHA-256 and executable bits for
runtime/tests plus Makefile; documentation and generated bytecode are excluded.

Independent review reproduced and fixed failure-path defects before this candidate.
Read the tool's precise storage, concurrency, identity, and truth/approval boundaries.
The companion `flightdeck-examples` repository contains the Linux validation harness.
